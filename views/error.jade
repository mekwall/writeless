!!! 5
html
  head
    meta(charset='utf-8')
    meta(httpEquiv='X-UA-Compatible', content='IE=edge,chrome=1')
    title= 'Error ' + errorCode + ' - ' + configs.title
  body
    - if (errorCode == 404)
      h1 Error
      h2 File not found!
      span The page you were looking for was not found.
    - if (errorCode == 500)
      h1 Error
      h2 Internal server error!
      span You must have broke something!